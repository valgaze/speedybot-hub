{
  "name": "speedybot-hub",
  "bin": {
    "speedybot-hub": "./cli/cli.js"
  },
  "version": "1.0.0",
  "devDependencies": {
    "@babel/core": "^7.18.2",
    "@babel/preset-env": "^7.18.2",
    "@babel/preset-typescript": "^7.17.12",
    "@cloudflare/workers-types": "^3.10.0",
    "@types/jest": "^27.5.1",
    "isomorphic-fetch": "^3.0.0",
    "jest": "^28.1.0",
    "ts-jest": "^28.0.3",
    "typedoc-theme-category": "^1.0.7",
    "typedoc-theme-hierarchy": "^1.2.1",
    "typescript": "^4.7.2",
    "wrangler": "2.0.6"
  },
  "private": true,
  "scripts": {
    "docz": "npm run docs:h && open http://localhost:1337 && cd api-docs && npx serve -p 1337",
    "docs:h": "rm -rf api-docs 2>/dev/null && npx --yes typedoc --media docs/assets --out api-docs --plugin ./node_modules/typedoc-theme-category/dist/index.js --theme category src/lib/index.ts",
    "docs": "rm -rf api-docs 2>/dev/null && npx --yes typedoc --media docs/assets --out api-docs src/index.ts",
    "build": "tsc src/index.ts",
    "test": "npx jest",
    "serve": "npx wrangler dev src/index.ts",
    "start": "npm run serve",
    "dev": "npm run serve",
    "deploy": "npm run publish",
    "deploy:prod": "wrangler publish --minify",
    "publish": "wrangler publish",
    "build:lib": "tsc --project tsconfig_lib.json",
    "append": "echo \"\n\" >> wrangler.toml && echo [vars] >> wrangler.toml && echo TOKEN = \"$1\" >> wrangler.toml"
  }
}
